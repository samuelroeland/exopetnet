    <p><%= link_to "Back to list", animals_path %></p>

    <p><%= link_to "Back to main page", root_path %></p>

<div class="show-animal-card m-3">
  <% if @animal.image.attached? %>
    <%= cl_image_tag @animal.image.key %>
    <% end %>
    
  <div class="show-animal-card-infos">
    <h2><%= @animal.name %></h2>
    <h5><%= @animal.specie %> - <%= @animal.origin %></h5>
    <p><%= @animal.description %></p>
    <p>owner: <%= @animal.user.username %></p>
  </div>

<<<<<<< HEAD
<%= simple_form_for [current_user, @booking] do |f| %>
  <%= f.input :arrival_date, as: :date, html5: true%>
  <%= f.input :end_date, as: :date, html5: true %>
  <%= f.submit %>
<% end %>
=======
  <div>
    <h2 class="card-trip-pricing"><%= @animal.price %>€</h2>
  </div>

  <div>
    <% if policy(@animal).edit? %>
    <p><%= link_to "Edit", edit_animal_path(@animal), class: "btn btn-success text-light"%></p>
    <% end %>
    <%= link_to "Delete", animal_path, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger text-light" %>
  </div>
>>>>>>> master

</div>
